import { CATALOG_DIAGNOSTICS, snapshotCatalog } from "../data/catalog";
import { POLICY_SNAPSHOT } from "../data/policies";
import { Channel, ChatMessage, Course, Decision, Profile, ScoredCourse, SoftwareExperience } from "./types";

const policyWords = /refund|recording|certificate|attend|live|zoom|vat|invoice|membership|cashback|transfer|support|enroll/i;
const NEGATION = /(?:don't want|do not want|not interested in|avoid|without|don't need|do not need|i don't want|i do not want|istemiyorum|ilgilenmiyorum)/i;
const CONCEPTS = [
  { value: "Grasshopper", kind: "tool" as const, pattern: /grasshopper/i },
  { value: "Rhino", kind: "tool" as const, pattern: /rhino/i },
  { value: "Midjourney", kind: "tool" as const, pattern: /midjourney/i },
  { value: "computational design", kind: "topic" as const, pattern: /computational design|parametric design/i },
  { value: "visualization", kind: "goal" as const, pattern: /visuali[sz]ation|rendering|render/i },
  { value: "fabrication", kind: "goal" as const, pattern: /fabrication|robotic/i },
  { value: "artificial intelligence", kind: "goal" as const, pattern: /\bai\b|artificial intelligence|generative ai|ai tools/i },
  { value: "architecture", kind: "topic" as const, pattern: /architecture|architect/i }
];
type Turn = { positive: Record<string, string>; negative: string[]; excludedTopics: string[]; excludedTools: string[]; software: SoftwareExperience[]; directAnswer: boolean; raw: string };

function isNegated(text: string, matchIndex: number) { const sentenceStart = Math.max(0, text.lastIndexOf(".", matchIndex) + 1); const before = text.slice(sentenceStart, matchIndex); const marker = before.search(NEGATION); return marker >= 0 && !/\bbut\b|\bonly want\b|\bactually\b/i.test(before.slice(marker)); }
function extractTurn(text: string, source: SoftwareExperience["source"]): Turn { const positive: Record<string, string> = {}; const negative: string[] = []; const excludedTopics: string[] = []; const excludedTools: string[] = []; for (const concept of CONCEPTS) { const match = concept.pattern.exec(text); if (!match) continue; if (isNegated(text, match.index)) { negative.push(concept.value); if (concept.kind === "tool") excludedTools.push(concept.value); else excludedTopics.push(concept.value); } else if (concept.kind === "tool") positive.tool = concept.value; else if (concept.kind === "goal") positive.goal = concept.value; else if (concept.kind === "topic" && /architect/i.test(text)) positive.profession = "architecture"; } if (/beginner|complete beginner|start|new/i.test(text)) positive.level = "Beginner"; const software: SoftwareExperience[] = []; for (const tool of ["Rhino", "Grasshopper", "Midjourney"]) if (new RegExp(tool, "i").test(text) && !excludedTools.includes(tool)) software.push({ software: tool, status: /learn|start|want to use/i.test(text) ? "learning" : "uses", source }); return { positive, negative, excludedTopics, excludedTools, software, directAnswer: /would that change|does that change|should i choose|is that relevant/i.test(text), raw: text }; }
function emptyProfile(): Profile { return { goals: [], software_experience: [], negative_preferences: [], excluded_topics: [], excluded_tools: [] }; }
function buildProfile(userMessages: ChatMessage[]): Profile { const profile = emptyProfile(); for (const [index, message] of userMessages.entries()) { const turn = extractTurn(message.content, index === userMessages.length - 1 ? "current_request" : "history"); if (turn.positive.profession) profile.profession = turn.positive.profession; if (turn.positive.level) profile.level = turn.positive.level; if (turn.positive.goal) profile.goals = [turn.positive.goal]; profile.negative_preferences = [...new Set([...profile.negative_preferences, ...turn.negative])]; profile.excluded_topics = [...new Set([...profile.excluded_topics, ...turn.excludedTopics])]; profile.excluded_tools = [...new Set([...profile.excluded_tools, ...turn.excludedTools])]; for (const item of turn.software) { const existing = profile.software_experience.find(x => x.software === item.software); if (existing) Object.assign(existing, item); else profile.software_experience.push(item); } } return profile; }
function answerFor(text: string, channel: Channel) { let answer = ""; if (/refund/i.test(text)) answer = POLICY_SNAPSHOT.refund; else if (/recording|attend|live|zoom/i.test(text)) answer = `${POLICY_SNAPSHOT.liveFormat} ${POLICY_SNAPSHOT.attendance} ${POLICY_SNAPSHOT.certificate}`; else if (/certificate/i.test(text)) answer = POLICY_SNAPSHOT.certificate; else if (/price|cost|eur|expensive|discount|member/i.test(text)) answer = `${POLICY_SNAPSHOT.commercial} ${POLICY_SNAPSHOT.membership}`; else if (/cashback/i.test(text)) answer = "Cashback is UNKNOWN in the approved policy snapshot, so I can't confirm it."; else answer = "I can help you find a suitable PAACADEMY learning path. What would you like to learn or apply in your work?"; return channel === "instagram" ? answer.split(". ")[0] + "." : channel === "whatsapp" ? answer.slice(0, 300) : answer; }
function goalMatches(goal: string | undefined, course: Course) { if (!goal) return false; const text = course.searchableText; if (goal === "artificial intelligence") return /\bai\b|artificial intelligence|generative ai|midjourney|stable diffusion|machine learning/.test(text); if (goal === "visualization") return /visuali[sz]|render|image|animation/.test(text); if (goal === "fabrication") return /fabrication|robotic|robot|material system/.test(text); return text.includes(goal.toLowerCase()); }
function rank(profile: Profile, current: Turn): ScoredCourse[] { const scored = snapshotCatalog.listCourses().map(course => { const haystack = course.searchableText; const excludedValue = profile.excluded_topics.concat(profile.excluded_tools).find(value => haystack.includes(value.toLowerCase())); if (excludedValue || /inactive|archived|draft|passive/i.test(course.content_status)) return null; let score = 20; const reasons: string[] = []; const requestedGoal = current.positive.goal ?? profile.goals[0]; if (goalMatches(requestedGoal, course)) { score += 40; reasons.push("GOAL_MATCH", "TOPIC_MATCH"); } if (profile.level && course.level.toLowerCase() === profile.level.toLowerCase()) { score += 20; reasons.push("LEVEL_MATCH"); } if (profile.profession && course.searchableText.includes(profile.profession)) { score += 8; reasons.push("PROFESSION_MATCH"); } for (const software of profile.software_experience) if (course.searchableText.includes(software.software.toLowerCase())) { score += 12; reasons.push("SOFTWARE_MATCH"); } if (/apply|workflow|directly|concept design/i.test(current.raw) && /architecture|design/.test(course.searchableText)) { score += 10; reasons.push("DESIRED_OUTPUT_MATCH"); } if (course.prerequisites.length === 0) reasons.push("PREREQUISITE_MATCH"); if (/live|workshop/i.test(current.raw) && course.format === "Workshop") { score += 6; reasons.push("FORMAT_MATCH"); } if (course.delivery_state === "UPCOMING") { score += 5; reasons.push("UPCOMING_BONUS"); } return { ...course, score, reasonCodes: reasons }; }).filter((course): course is ScoredCourse => Boolean(course)).sort((a, b) => b.score - a.score); return scored.slice(0, 10); }
function selectAlternative(candidates: ScoredCourse[], primary: ScoredCourse | null) { return candidates.find(course => course.id !== primary?.id && course.course_family !== primary?.course_family) ?? null; }
function explain(course: ScoredCourse) { const price = course.priceEur === null ? "price is not available in this snapshot" : `€${course.priceEur}`; return `Based on your current request, I'd start with **${course.title}** (${course.format}, ${course.level}, ${price}). It matches ${course.reasonCodes.filter(code => ["GOAL_MATCH", "TOPIC_MATCH", "LEVEL_MATCH", "SOFTWARE_MATCH", "PROFESSION_MATCH", "DESIRED_OUTPUT_MATCH"].includes(code)).join(", ") || "the retrieved course profile"}. Delivery state: ${course.delivery_state}.`; }
export function decide(messages: ChatMessage[], channel: Channel): Decision { const userMessages = messages.filter(m => m.role === "user"); const latest = userMessages.at(-1)?.content ?? ""; const current = extractTurn(latest, "current_request"); const profile = buildProfile(userMessages); const previous = userMessages.length > 1 ? decide(userMessages.slice(0, -1), channel) : null; const isPolicy = policyWords.test(latest); const directQuestion = current.directAnswer && Boolean(previous?.primary); const candidates = rank(profile, current); const primary = candidates[0] ?? null; const alternative = selectAlternative(candidates, primary); const changed = Boolean(previous?.primary && primary && previous.primary.id !== primary.id); const changeReasons = changed ? primary.reasonCodes.filter(code => !previous!.primary!.reasonCodes.includes(code)) : []; const answer = isPolicy ? answerFor(latest, channel) : directQuestion ? `${profile.software_experience.find(x => x.software === "Rhino")?.status === "uses" ? "Yes" : "No"} — your current software experience is relevant, but it does not create an unsupported prerequisite chain. ${primary ? explain(primary) : ""}` : primary ? `${explain(primary)}${alternative ? ` Alternative: **${alternative.title}**.` : ""}${changed ? ` I changed this recommendation because ${changeReasons.join(", ").toLowerCase()}.` : ""}` : answerFor(latest, channel); const state = isPolicy ? "POLICY" : primary ? "RECOMMENDATION" : "QUALIFICATION"; const missing = isPolicy || primary ? [] : ["desired_goal", "experience_level"]; return { primaryIntent: isPolicy ? "policy_question" : /compare|difference/i.test(latest) ? "course_comparison" : /buy|register|enroll|take/i.test(latest) ? "purchase_intent" : "course_discovery", secondaryIntent: isPolicy ? "course_discovery" : null, state, conversation_state: state, entities: current.positive, profile, missingInformation: missing, nextBestAction: isPolicy ? "ANSWER_POLICY" : primary ? "SHOW_RECOMMENDATION" : "ASK_ONE_HIGH_VALUE_QUESTION", candidates, candidate_count: candidates.length, primary, alternative, confidence: isPolicy || primary ? "HIGH" : "MEDIUM", reasonCodes: [isPolicy ? "APPROVED_POLICY_SNAPSHOT" : "CURRENT_EXPLICIT_REQUEST", "RELEVANCE_GT_FIT_GT_UPCOMING", ...(profile.excluded_topics.length || profile.excluded_tools.length ? ["EXCLUDED"] : []), ...(changed ? ["RECOMMENDATION_CHANGED"] : [])], sourceProvenance: isPolicy ? ["data/policies.ts:verified-policy-snapshot"] : ["data/snapshots/all-courses-with-all-details.csv", "data/snapshots/course_intelligence.json", "lib/engine.ts:deterministic-joined-ranking"], answer, recommendation_changed: changed, previous_primary: previous?.primary?.title ?? null, new_primary: primary?.title ?? null, change_reasons: changeReasons, catalog_diagnostics: CATALOG_DIAGNOSTICS }; }
